package com.smartjob.entity.enums;

/**
 * Represents user roles in the SmartJob system.
 *
 * CANDIDATE — Can manage profile, search jobs, view matches, apply for jobs
 * RECRUITER — Can create/manage jobs, view applicants, update application status
 */
public enum Role {
    CANDIDATE,
    RECRUITER
}
